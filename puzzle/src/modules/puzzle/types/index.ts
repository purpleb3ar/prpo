export enum Visibility {
  Private = 'private',
  Public = 'public',
  InviteOnly = 'invite-only',
}

export enum ProcessingState {
  Created = 'created',
  Processing = 'processing',
  Done = 'done',
}

export enum PuzzleAsset {
  Specification = 'specification',
  Spritesheet = 'spritesheet',
  Thumbnail = 'thumbnail',
}
